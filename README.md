# sway-output-profiles
Set (or toggle between) multiple output profiles for the sway window manager

## Dependencies

- yaml (`pip3 install pyyaml`)

## Configuartion

- Edit `profiles.yaml`
- (Optional) Exit global variables in the script